import React, { Component } from 'react';
import Publish from './components/Publish';

export default class PublishTasks extends Component {
  render() {
    return (
      <div>
        <Publish />
      </div>
    );
  }
}
